// 2026 01 22 목

throw   < 예외를 직접 발생시킨다 .
throws 메서드를 호출한 쪽으로 예외를 던짐

내부클래스    
클래스 멤버
-필드 ,생성자 , 메소드

 내부클래스는 클래스 내부에 선언된 클래스를 의미 // 두 클래스가 서로 긴밀한 관계가 있을때 사용
 ex) 상자 안에 작은 상자가 들어있는 것과 같은 구조
 인스턴스 - 객체라고 이해 
 
 인스턴스 내부클래스 ,정적 내부클래스 , 지역클래스 , 익명클래스
 
 추상클래스  - 
 제너릭 사용하는 이유 
 1. 타입안정성 //
 제너릭을 쓰면 엉뚱한 데이터가 들어오는 것을 컴파일 단계에서 미리 막을 수 있음 .(빨간줄)
 
 2. 번거로운 형변환 생략
 과거에는 데이터를 꺼낼 때 마다 String 처럼 형변환을 해야 했지만 제너릭을 쓰면 자바가 알아서 처리해줌
  
 >> 자주 사용하는 제너릭 기호 (관례)<<
 이 기호들은 규칙은 아니지만 전 세계 개발자들의 '약속' 
 
 <T> : Type (일반적인 타입)
 <E> : Element (리스트 같은 컬렉션의 요소)
 <K> : Key(맵의 키)
 <V> : Value(맵의 값

  제너릭 와일드카드 <?>
  의미 : 어떤 타입이든 다 받을 수 있다
  특징 : 타입에 의존하지 않는 단순 출력이나 개수 확인같은 작업에 사용
  제약 : 타입을 알 수 없으므로 null 외에는 요소를 추가(add) 할수 없다
  
  <?>   모든게 올수 있음 
   <? extends T>  상한제한 T 와 그 자식들 데이터 읽기 전용
   목적 : 특정 타입의 자식들을 모두 받아 데이터를 안전하게 읽기 위해 사용
   <? super T> 하한 제한 T와 그 부모들 데이터 쓰기 전용
   목적 : 특정 타입의 부모들을 받아 데이터를 안전하게 저장하기 위해 사용
  
  
  
  
  
  
  
  
  